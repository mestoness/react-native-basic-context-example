import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  card: {
    padding: 20,
    borderWidth: 1,
    borderColor: '#d4d2d2',
    borderRadius:5
  },
  cartTitle:{
    textTransform:"capitalize"
  }
});
